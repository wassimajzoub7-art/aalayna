/* Set to an HTTPS endpoint you own to collect aggregate events across visitors.
   Keep empty until a collector is provisioned. Never put API secrets here. */
window.AalaynaAnalyticsConfig = { endpoint: '' };
